# HANDOFF - AlKarama Ibn Sina Tourisme website

Read this to continue the build without re-reading the old chat.

## The deal
- Client: owner of AlKarama Ibn Sina Tourisme, a travel agency in Tunis, Tunisia.
- Price: 2000 TND total = 1150 (frontend) + 850 (backend/admin panel client can self-edit).
- Client saw a Lovable demo and liked it. We build custom React (free hosting, own admin panel, no monthly fee).
- Design inspired by elitemasar.com (layout only, NOT a copycat). Saved copy: `D:\EliteMasar Travel Agency (25_06_2026 14_34_09).html`.

## Business info (real, in src/data.ts)
- Name: AlKarama Ibn Sina Tourisme. Tagline: "Une autre approche pour faire du tourisme".
- Phones: 93 996 300 and 90 007 555. WhatsApp intl: 21693996300.
- Address: Carrefour Market Avicenne, Centre Commercial Lawand Ibn Sina, Tunis 2066.
- Rating: 5.0 from 15 Google reviews.
- Facebook link set. Instagram: https://www.instagram.com/alkarama.ibnsina.agency/ (real, confirmed by Adam).

## Brand
- Colors: gold #C9A227, charcoal #222, cream #FAF8F3 (in tailwind.config.js).
- Logo in nav is TEXT only ("ALKARAMA" + "TOURISME · IBN SINA"). Client hated the logo image and the "A" box. Do not add an image logo unless Adam sends a clean white one.
- Fonts: Playfair Display (headings), Poppins (body).

## Services (4)
1. Omra & Hajj (partner: Al Morchidoun / المرشدون)
2. Hotels & Sejours
3. Visas (Pays Arabes, Europe/Schengen, Arabie Saoudite)
4. Billets avion et bateau (flight + ferry)

## Mobile
- Hero's offer card (the small box with badge/price/book button) was completely hidden below `lg` breakpoint (`hidden lg:block`) - invisible on phones. Fixed: now shown on all sizes, stacked below the headline on mobile, smaller image/padding on small screens. Section height changed from fixed `h-screen` to `min-h-screen` (mobile can grow if content is taller than the viewport) while staying `lg:h-screen` (exact one-viewport hero unchanged on desktop). Partner logo band flows normally after the content on mobile instead of being absolutely pinned to the bottom, to avoid overlap risk.

## Homepage (built, in order)
Hero (4 rotating service slides, Ken Burns zoom, side offer card with urgency -> WhatsApp, white partner logos overlaid at bottom) -> Omra (3 packages Essentiel/Confort/VIP, VIP highlighted gold "Le + choisi", prices for 4/3/2 people per room) -> Hotels (3 cards Tunisie/Mecque/Istanbul, hover-zoom photo, overlay stars+name+location+price, green Reserver corner) -> Visas (3 package cards with "Inclus dans votre dossier" bullets + Demander le visa) -> Billets (2 image cards plane+ferry, green Book now) -> WhyUs -> Reviews (3 Google 5-star) -> Contact (Google map + WhatsApp) -> Footer.
- Nav: Accueil, Omra & Hajj, Hotels, Visas, Billets + green "Contactez-nous" button -> WhatsApp. Transparent glass on scroll.
- Social rail on RIGHT side, brand colors (FB blue, IG gradient, WhatsApp green). Chatbot icon/mock window REMOVED entirely per Adam ahead of the client demo (was previously a visual-only preview, no real bot) - `Icon.chat` and all `chat.*` i18n keys deleted too.
- All CTAs go to WhatsApp with a prefilled message.

## Real content captured
- Umrah: real flyer data now in src/data.ts. `umrahPacks` (3 featured, shown on homepage): Essentiel + VIP (both dates 20/23/27 juil + 3/6/10 aout, same 2 hotel tiers: Ramada 4*/Arkan Al Manar and Abraj Al Safwa 5*/Arkan Al Manar) + Mawlid Ennabaoui (25 aout, Manarat Ghazah 4*/Arkan Al Manar, cheapest of 7 tiers for that date). `umrahMoreDepartures` (6 more real hotel tiers for 25 aout, not displayed anywhere yet, waiting on the "Decouvrir tout" full listing page). Adam picked the 2-normal-plus-1-VIP homepage layout and confirmed grouping same-price dates under one card with a date badge instead of duplicating cards.
- Real hotels: Four Seasons Hotel Tunis (Gammarth, 1550 DT), Makkah Clock Royal Tower Fairmont (630 DT), Swissotel The Bosphorus Istanbul (915 DT). Real photos in public/hotels/.
- Real reviews (3, from Google): Essaim Ben Chorod, Chaib Amna, Rafia Temimi. In src/data.ts reviews.
- Real Instagram: https://www.instagram.com/alkarama.ibnsina.agency/
- Hero background/card images updated with real photos: Omra/Hajj slide background = hajj.png (small offer card keeps old mecca.jpg), Visas slide background = vienna-austria.jpg, small offer card = visas-france.jpg (Schengen offer). Homepage Visas section "Pays Arabes" card image = dubai.png. `HeroSlide` type now has an optional `cardImg` separate from the main background `img`.
- Egypt packages (spreadsheet `D:\doc site web.xlsx`): Cairo-Sharm 3990 DT, Cairo-Alexandria 2790 DT. Turkey packages (Istanbul/Antalya). Visa country lists. Not all surfaced on homepage yet.
- Partner logos (white PNG in public/partners/): Saudi Tourism, Al Morchidoun, Inspiring Tunisia, IATA, Go Turkiye. Amadeus = text wordmark only (no logo file). Source logos on `D:\` (IATA logo.PNG, saudi tourism authoirty logo.PNG, inspiring tunia logo.PNG, go turkey logo.PNG). Whitening script pattern: Pillow, corner-detect bg (white/black/transparent), recolor to white, alpha threshold.

## Routing (built)
- react-router-dom, `BrowserRouter`. Routes: `/` (HomePage, all sections), `/omra` (full Omra/Hajj listing, 9 packs), `/hotels` (full hotel listing, current 3 + "more on request" note), `/visas` (full detail per visa category incl. country lists), `/reserver-billet` (ticket booking form, reads `?type=avion|bateau`).
- SPA fallback configs added for whichever host is picked later: `public/_redirects` (Netlify) and `vercel.json` (Vercel).
- Nav links use `to="/#omra"` style (react-router Link); `HomePage.tsx` scroll-to-hash on mount/hash change so cross-page anchor nav works. `App.tsx` has a `ScrollToTop` on route change (skipped when there's a hash).
- "Decouvrir tout" (`DiscoverAll` in Sections.tsx) is now a filled pill button (was plain text link) linking to the real pages above.
- Reusable card components extracted for reuse between homepage teasers and full pages: `OmraPackCard`, `HotelCard` (both exported from Sections.tsx).

## Ticket booking form (built, mailto only - Adam rejected Formspree)
- `/reserver-billet` collects only: type (avion/bateau), name, phone, destination, depart date, return date (optional), people count, notes (optional). No fictional fields per Adam's "minimum info, rest by phone" instruction.
- Originally built against Formspree (free form-to-email relay) with a mailto fallback. Adam rejected using any third-party tool ("we don't want any additional tools"), so Formspree was removed entirely. It now ALWAYS opens the visitor's own email app via `mailto:` to `bookingEmail` (placeholder in `src/data.ts`, needs Adam's real address) with the form fields pre-filled in the body.
- IMPORTANT tradeoff Adam should understand: a static site with no backend cannot silently deliver an email by itself; it can only ask something else to do it (a form relay service, or a backend). `mailto:` is the zero-account option, but it is NOT silent: it opens the visitor's own email app and THEY must click send there (can fail quietly if they have no mail app configured, common on Android). A truly silent "just lands in the inbox" experience needs either a form relay service (Formspree/EmailJS, which Adam declined) or a real backend email sender (Supabase Edge Function + an email API like Resend) - that's part of the PENDING backend phase below, not built yet.
- Booking page banner is dynamic: crossfades between the plane and ferry photo depending on the selected ticket type, plus a subtle Ken Burns zoom (same treatment now applied to all `PageBanner` instances: Omra/Hotels/Visas pages too) so it reads less like a generic stock photo.
- Billets section cards and the Hero billets-slide CTA link here instead of opening WhatsApp directly (`HeroSlide.card.formLink` field added for that special case).

## Hotels search (built, basic/preview - real system integration is future work)
- `/hotels` has a search bar (destination text, check-in/out dates, people count). Destination filters the 3 real hotels client-side (genuinely works). Dates/people are NOT filtered against anything real (no availability data exists) - they're collected and relayed via the WhatsApp CTA message instead of pretending to check live availability.
- Clearly labelled to the visitor as "real-time availability coming soon." Adam's plan: once backend work starts, wire this to whatever booking system the client already uses so it shows real availability - noted as pending below.

## Pre-deploy fixes (2026-07-02, verified via actual screenshots, not just code review)
- Hotels page search bar had a `-mt-24` negative margin meant to overlap the banner photo, but it actually overlapped the banner's title/subtitle text, breaking the design. Removed the overlap trick; search bar now sits in normal flow below the banner.
- Booking form (`/reserver-billet`): submit handler used `window.location.href = "mailto:..."`, which in some browsers triggers a visible page reload/reset before the "almost done" confirmation can show (confirmed via testing). Fixed by triggering the mailto via a throwaway `<a>` element's `.click()` instead, which doesn't reload the page.
- `vite.config.ts` now reads `server.port` from `process.env.PORT` (falls back to 5173) so the preview tooling's auto-port assignment actually lands where Vite listens - fixes flaky screenshot/preview tool from earlier sessions. Doesn't affect Adam's normal `npm run dev` (still defaults to 5173).
- Confirmed via screenshots at 390px (mobile) and 1440px (desktop): hero offer card now visible and correctly stacked on mobile, hotels search bar and results grid render cleanly at both sizes, chatbot icon is gone, all internal routes (`/`, `/omra`, `/hotels`, `/visas`, `/reserver-billet`) load without console errors, and every `<a href>` on every page was enumerated and checked (no dead/empty hrefs).
- `npm run build` (the command Vercel will run) verified to succeed cleanly.

## Client change round (2026-07-03, local only, NOT pushed)
Applied from the client's visual remarks + logo/partner images Adam relayed:
- LOGO: replaced the text nav logo with the real gold falcon logo. Source `D:\alkarama logo.jpeg` processed with Pillow (crop + knock out off-white bg to transparent). Two outputs in `public/brand/`: `logo-full.png` (charcoal text, for light backgrounds) and `logo-white.png` (dark text recolored white, used in the dark navbar + footer). Slight soft halo on the white variant, acceptable; regenerate from a clean transparent source if the client sends one.
- "Omra et Hajj" renamed to "Packs Omra" everywhere (nav, hero slide, omra kicker/title). "Hajj" word dropped per client.
- Omra section retitled "Nos Formules Omra saison 2026-2027". Packs replaced with the client's 5 tiers: Classique / Confort / Prestige / VIP+ / A la Carte. IMPORTANT (Adam's steer: "Mawlid is a DATE, not a pack; the old flyers already hold all the prices, rearrange them"): each tier reuses the client's REAL published flyer data (hotels + prices + dates) remapped as an ascending comfort ladder. Mapping used -> Classique = summer Essentiel 4* (Ramada Dar Faiezine, 4250/4650/5250, departs 20/23/27 juil + 3/6/10 aout); Confort = Mawlid Manarat Ghazah 4* (4550/4850/5300, 25 aout); Prestige = Mawlid Ash-Shuhada 5* (5400/5850/6400, 25 aout); VIP+ = Mawlid Abraj Al Safwa 5*, highlighted (5850/6300/6950, 25 aout); A la Carte = custom, "sur demande". So Classique is a summer departure while Confort/Prestige/VIP+ are the 25-aout Mawlid departure (dates shown per card). NO invented numbers; the client should confirm the final ladder + ideally give one clean price grid per tier across all dates. `OmraPackCard` renders a price-on-request note when `prices` is empty, plus an optional `notIncluded` line ("timbre de voyage"). `UmrahPack` type gained optional `notIncluded` + `priceNote`. `umrahMoreDepartures` trimmed (removed Ash-Shuhada + Abraj Al Safwa, now featured) to avoid dupes on `/omra`; still holds Al Ayyam, Anfanti, Ramada, Hilton Marriott.
- NEW section `TravelPacks` (id `#packs`, EliteMasar "Destinations Packs" style image cards): Voyage Organise / Transfert / Location de Bungalows -> WhatsApp. Data in `data.ts` `travelPacks`. Images are PLACEHOLDERS (istanbul/dubai/resort). Added nav item "Nos Packs".
- NEW `Partners` section: color logos (client wanted color, not the white hero overlay). Copied color sources from `D:\` into `public/partners/*-color.*`; list in `data.ts` `colorPartners`. White hero-overlay logos untouched.
- NEW `Connect` section (id `#connect`): faithful replica of EliteMasar's "Connect With Us" cards (per client request to match that screenshot). 3 rich cards - Facebook (cover+avatar+mock post+reactions), Instagram (avatar+stats+3x2 photo grid+caption), TikTok (cover+avatar+video thumb). AlKarama branding: avatar = `public/brand/avatar.png` (charcoal circle + white logo, generated via Pillow), name "AlKarama Ibn Sina", cover = logo-white on charcoal gradient. Every "Follow" link + footer button -> the REAL page (business.facebook / instagram / tiktok). PLACEHOLDER follower/like/view numbers (3 200 / 18,5k / 12,4k etc.) + mock post text -> flagged in a code comment, client should replace with real figures. Photos are AlKarama assets (hajj/mecca/resort/istanbul/cappadocia/dubai/sharm) so it isn't a copy of EliteMasar. X + YouTube not shown here yet (no URLs).
- Transfert pack now uses a real luxury-car photo `public/packs/transfert.jpg` (black Mercedes AMG, free Pexels stock, commercial-use OK, pexels.com/photo/6152812) + desc updated to "voiture de luxe avec chauffeur".
- Social everywhere (SocialRail + footer + Connect) auto-hides any platform whose URL is blank. `business` in `data.ts` gained `tiktok` (real @alkaramaibnsina), `x` and `youtube` (both "" for now -> set the string when the client sends them and they light up automatically). New icons `tiktok/youtube/x` in `Icons.tsx`.
- DATA STILL NEEDED FROM CLIENT to finalize: confirm/complete the Omra price ladder (ideally one price grid per tier across all season dates); real X handle + YouTube channel URL; real photos for the 3 travel packs (currently on-brand placeholders: cappadocia / dubai / resort) + their prices if any.
- `npx tsc --noEmit` clean, no console errors, all images load (verified in preview).

## Client round 3 (2026-07-07, local only, NOT pushed at time of writing)
NOTE ON PARALLEL SESSIONS: a second Claude session (different chat) did "Client round" and "Client round 2" work directly on this repo in between my rounds (gold palette, scrolling partner banner, destinations/packs redesign with Bali+Maldives, YouTube video section, hotels moved up, `heroPartners`/`destinations`/`includedServices` replacing my old `travelPacks`/`colorPartners`). Only ONE session should drive this repo at a time going forward - Adam confirmed sessions don't run concurrently, but if that ever changes, check `git log` before editing, someone else's uncommitted structure may differ from what's described in older HANDOFF entries above.

This round, on top of that "round 2" state:
- Partners row (`heroPartners` in data.ts, the scrolling banner in `Hero.tsx`): ONLY Al Morchidoun stays in color (`morchidoun-color.png`). Tunisie Telecom was colored before; generated `public/partners/tunisie-telecom-white.png` (recolored every opaque pixel to white, background was already transparent) and removed its `color: true` flag.
- Omra "Decouvrir tout" -> `/omra` is now a CALENDAR page (`src/pages/OmraPage.tsx` rewritten). Two month-grids (July + August 2026, derived from data so it self-adjusts), dates with a real departure are clickable/highlighted, clicking a date filters `OmraPackCard`s to that date. Packs with no fixed date (A la Carte) always show below in a separate "Formule sur mesure" block. Driven by a new `UmrahPack.departureDates?: string[]` field (ISO `YYYY-MM-DD`) in `data.ts` - every pack now carries real ISO dates instead of just a display label.
- Omra pack names: removed hotel names as pack titles (client's instruction - name must be Classique/Confort/Prestige/VIP+/A la Carte only, never a hotel name). Renamed on `umrahMoreDepartures`: "Al Ayyam" -> "Confort", "Ramada" -> "Confort", "Hilton Marriott" -> "Prestige" (grouped by star rating with the existing tier of that level; the real hotel name is still visible inside each card's service list, just not as the H3 title). This means multiple cards can share a tier name on the same date now (by design - they're alternate hotel options at the same comfort level).
- Bali photo replaced: old one was a generic beach; new `public/packs/bali.jpg` is an aerial shot of Bali's Tegallalang rice terraces with dense tree/palm cover (free Pexels stock, commercial-use OK, pexels.com/photo/5933066), matches Adam's "island with a lot of trees, not a basic beach" ask.
- Transfert service reworked earlier already used a luxury car; kept as-is (client separately asked for "luxury car with chauffeur" - was already done in a prior round).
- All customer-facing CTA buttons unified to GREEN (Adam: "any call-to-action button should be green"). Changed: Hero card book button, Omra highlighted-card book button (was charcoal), Contact WhatsApp CTA, HotelsPage "more hotels" WhatsApp CTA, Visa "apply" buttons (homepage + `/visas` page) - all were gold, now `btn-green` / emerald. Left as-is (not customer conversion CTAs): admin panel save/login buttons, hotel search "Rechercher" form button - these are utility/internal, not marketing CTAs.
- Visa button text changed from "Demander le visa" to "Plus d'informations" per Adam (still opens WhatsApp with a prefilled message, just softer framing before the ask).
- Footer: added a real credit line linking to `https://krateon.tn` - "Site, SEO et presence IA concus par Krateon.tn" (EN: "Website, SEO and AI presence designed by Krateon.tn"), phrased broadly per Adam's ask so it signals Krateon did more than just the website (SEO/AI systems too), positioned as free advertising for the agency.
- `npx tsc --noEmit` clean, no console errors, all images load (verified in preview: calendar navigation, tier renames, partner colors, footer link, Bali image).

## Client round 4 (2026-07-10, local only, NOT pushed)
Front-end-only visual pass from the client's remarks after seeing the live site:
- LOGO: replaced with the client's new provided file (`D:\alkarama updated website.jpeg`). Reprocessed with Pillow (background knocked out to transparent, cleaned of JPEG speckle noise, cropped tight) into `public/brand/logo-full.png` (color, for light backgrounds) and `logo-white.png` (solid white fill + a thin dark outline around every letter/shape for definition on the dark navbar, per client's "pop up / borders on the letters" ask). Same filenames as before, so no code changes needed, just the asset swap.
- HERO OFFER CARD (`Hero.tsx`): restructured so the photo is much taller (h-64/h-80) and now carries the title, subtitle and "a partir de" price directly on the image (dark gradient overlay for legibility). The frosted-glass strip below the photo now holds ONLY the book/WhatsApp button, per client ask.
- HERO ROTATING BADGE (the gold pill that cycles "Packs Omra / Hotels et Sejours / Visas / Billets..."): text changed from white to charcoal (black on yellow) for readability.
- PARTNER LOGOS (`data.ts` heroPartners): switched to official color versions (saudi-color, tunisia-color, iata-color, turkiye-color, tunisie-telecom original color) instead of the whitened/monochrome set. Al Morchidoun was already color. Ooredoo and Star Assurances stay white-only: their source files on disk contain ONLY white pixels (verified via Pillow), no color art exists yet - need real logos from the client to color those two.
- STICKY NAVBAR (`Navbar.tsx`): nav links changed from `text-white/90` to solid `text-white` for better visibility.
- SOCIAL RAIL (`SocialRail.tsx`): icon buttons changed from `rounded-full` to `rounded-xl` (squarish "app icon" look per client's iPhone reference). Icon glyphs themselves (Facebook/Instagram/WhatsApp/TikTok/YouTube/X in `Icons.tsx`) were reviewed and already match each brand's current official mark, so left as-is.
- OMRA PACK CARDS (`Sections.tsx` `OmraPackCard`): photo enlarged (h-56) and now carries the date badge, pack name and star rating as an overlay directly on the image; occupancy counts (4/3/2 pers.) replaced with small stacked head icons (new `Icon.person` in `Icons.tsx`); price numbers changed from the Playfair display font to a plain bold sans style; the "Prix/personne selon la chambre" note moved from above the price grid to below it, smaller text (matches the "Non inclus" line style).
- VIP+ BADGE: "Le + choisi" renamed to "Le plus populaire" / "Most popular" (`i18n.tsx` omra.popular).
- OMRA CALENDAR (`OmraPage.tsx`): available-date highlight changed from beige/gold to light green (emerald-100/emerald-500), including the legend swatch.
- DESTINATIONS SECTION ("Nos Packs", `data.ts` + `Sections.tsx` DestinationCard): added a placeholder "a partir de 1 950 DT" price on all 3 featured cards (Caire/Istanbul/Bali) - this exact figure was given by Adam himself as the example number, but should still be confirmed/replaced with real per-destination pricing before launch. Added a red "PROMO" ribbon on the Istanbul card. CTA button text changed from "Reserver" to "Plus d'infos" / "Get more info" on all 3.
- TICKET BOOKING FORM (`BookingFormPage.tsx`): added a required "with/without language guide" radio field (avec/sans accompagnateur), included in the mailto body. New i18n keys `booking.guide*`.
- WHY US SECTION (`Sections.tsx` WhyUs): removed the 6 feature tiles per client ask, section now shows only the kicker + slogan title ("Une agence, pas un intermediaire").
- `npx tsc --noEmit` clean. Verified via preview (DOM snapshot/inspect, not screenshots): navbar white text, logo loads, social rail squarish, destinations show price+promo+new CTA text, WhyUs stripped down, booking form has the guide field, Omra calendar cells render emerald-green for available dates, Omra cards show head icons + overlay photo header.
- NOT changed (out of scope per Adam's "front-end only" instruction this session): backend/admin/Supabase.

## Client round 5 (2026-07-10, same day follow-up, local only, NOT pushed)
Adam reviewed round 4 and sent corrections:
- LOGO: client wants the actual color logo (black text + gold falcon), not a white recolor. `public/brand/logo-white.png` now just a copy of `logo-full.png` - navbar/footer/Connect all show the real color logo. Worth keeping an eye on: over the transparent/bright parts of the hero photo the charcoal text may have lower contrast than a white logo would - flag to Adam if it reads poorly on his screen, easy to make a dedicated navbar variant later if so.
- HERO CARD: price moved back OFF the photo and into the frosted-glass strip, beside the book button (previous round had put it on the photo by mistake). Title/subtitle stay on the photo.
- PARTNER LOGOS: several color source files (`saudi-color.png`, `tunisia-color.png`, `iata-color.png`, `turkiye-color.png`) turned out to have a solid white (or black, for Turkiye) rectangle baked into the file instead of transparency. Fixed with a flood-fill knockout from the image borders (safe technique - doesn't touch enclosed white details like the IATA globe's meridian lines). `ooredoo.png` and `star-assurances.png` had NO color source at all (pure white silhouettes) - recolored programmatically to their well-known official brand colors (Ooredoo red, Star Assurances green). Worth a quick sanity check against the client's own files if he ever sends the originals, but both now visually match the real public logos. Tunisie Telecom and Al Morchidoun were already correct, untouched.
- OMRA HOMEPAGE CARDS: removed the `md:-mt-4 md:mb-4` offset that made the VIP+ (highlighted) card sit higher/lower than Classique and Prestige - all three now sit on the same baseline, and their WhatsApp buttons align exactly (verified: same top/bottom pixel position at desktop width).
- DESTINATIONS/ISTANBUL PROMO: card now visually pops beyond the small red "PROMO" tag - added a gold ring, a gold glow shadow, a slight scale-up on desktop, and a pulsing star icon in the badge.
- BOOKING FORM: swapped the "with/without language guide" field for "with/without luggage" (`booking.baggage*` keys) - the guide field wasn't what Adam meant and he flagged it clearly.
- WHY US: now a full designed banner (dark charcoal-gold gradient over `public/brand/banner.png`, gold kicker pill, large centered slogan, gold underline) instead of plain text on a flat background.
- CONTACT SECTION: address/phone/hours icons now have distinct colors (red/green/blue tinted backgrounds) instead of one uniform gold-on-sand look.
- SOCIAL ICONS: enlarged inside their buttons (h-5 -> h-7) in both the side rail and the footer's 4 icons; footer icon buttons changed from round to the same squarish `rounded-xl` shape as the side rail, so both match.
- NAVBAR: the "Contactez-nous" button replaced with a phone icon + the real number (`93 996 300`) so desktop visitors see it directly, still opens WhatsApp on click.
- SECTION KICKERS: the small uppercase labels above every section title (Hotels et Sejours, Nos Packs, Visas, Video, Packs Omra, etc.) now render as a gold pill with charcoal text, matching the hero's rotating badge style, instead of plain colored text.
- `npx tsc --noEmit` clean. Verified via preview DOM checks (not screenshots): logo src, nav CTA text, hero card price position, Omra card alignment (identical top/bottom across all 3 + buttons), Istanbul promo classes, contact icon colors, WhyUs banner image, social icon sizes.
- OUT OF SCOPE (business question, not implemented): Adam asked whether the "Powered by Krateon.tn" footer credit is enough self-promotion for Krateon on the client site - answered as a recommendation in chat, not a code change.

## Client round 6 (2026-07-10, same day, PUSHED to production)
Adam flagged that the round-4/5 partner logo fixes still looked broken and asked for the logo to revert to white. Root cause turned out to be important: **none of round 4/5 had been pushed yet**, so the client review that triggered this round was almost certainly looking at the still-live OLD assets (plain white rectangles behind every partner logo on the dark hero band) - not any of the previously-described fixes. That risk (doing local-only work across several rounds while the client keeps reviewing the live site) is why this round ends with an actual push.
- PARTNER LOGOS - real fix this time: `saudi-color.png`, `tunisia-color.png`, `iata-color.png`, `turkiye-color.png` were re-processed from the git-committed originals with a border-flood-fill transparency knockout (safe technique, verified with pixel-level checks, not just eyeballing - doesn't touch enclosed design details like the IATA globe's white gridlines). Saudi additionally had a genuine leftover artifact - a small stray gray blotch near the icon, confirmed via connected-component analysis and removed surgically (component color/position based, not a blanket effect) while preserving the logo's legitimate white "sail" negative-space shape. Ooredoo/Star Assurances recolored again (red/green) since restoring from git reverted that too. IMPORTANT LESSON LOGGED: an earlier over-aggressive "erode near-white pixels touching transparency" attempt during this round wrongly ate into IATA's legitimate white gridlines (they touch the globe's outer rim, same as real artifacts do) - reverted and replaced with the safer, already-proven border-flood approach. Also discovered these partner PNGs are cleanpng.com-style stock downloads (small resolution, some JPEG-artifact noise) - genuinely clean vector-quality logos would need Adam to source official brand assets directly if pixel-perfect quality is ever required.
- LOGO: reverted to a proper WHITE logo variant (not the color one from round 5) - regenerated fresh from `D:\alkarama updated website.jpeg` (white fill + thin dark outline for definition), since the black/charcoal text was unreadable against the dark hero/navbar as Adam found. Also sized up: navbar `h-12 sm:h-14` -> `h-14 sm:h-16`, footer `h-14` -> `h-16`.
- `npx tsc --noEmit` clean. Verified via preview: all partner logos load (naturalWidth confirms no broken images), logo renders at the bigger size from `/brand/logo-white.png`.
- PUSHED to `main` on Adam's explicit request (this round only - do not push future rounds without asking again).

## Client round 7 (2026-07-10, same day, PUSHED to production)
Adam said Saudi/IATA/Inspiring Tunisia were "still fucked" right after round 6's push, and asked for the OLD gold+white logo back (not the all-white one).
- ROOT CAUSE FOUND for the repeated "still broken" reports across rounds 6/7: `git checkout -- <file>` restores to the latest COMMIT, and once round 6 pushed a fixed version, later "restore the original then reprocess" attempts in this round were actually re-processing an ALREADY-transparent image (background pixels already near-black/transparent, not white), which silently broke the white-background-detection math (nearly produced a fully-opaque file at one point - caught immediately via direct numpy pixel checks before it could ship). Lesson: stop assuming `git checkout` means "pristine pre-session file" once something's been committed - check the commit being restored from.
- Also found and fixed the REAL remaining defect: `iata-color.png` still had ~611 tiny (~100-140px) near-white speckles scattered across it - leftover from a "cleanpng.com" watermark baked into the original stock download. Removed them surgically via connected-component analysis (isolated small near-white blobs only; the logo's legitimate white globe-gridline segments are much larger, 800-3000px, and were left untouched - verified by listing every remaining component's size/color after the cleanup). Saudi and Inspiring Tunisia were re-verified pixel-by-pixel (not just by eye - the image preview in this tool turned out to render unreliably/inconsistently across repeated edits of the same filename, so all "clean" claims from here on are backed by direct alpha-channel/connected-component numpy checks, not just a visual read) and are already properly transparent with no stray artifacts.
- LOGO: reverted to the ORIGINAL gold-falcon-plus-white-text logo from before the round-4 logo swap (restored `public/brand/logo-full.png` and `logo-white.png` from commit `b3dd7b7`, the "Client round" 2026-07-03/07 version) - this is the "gold and white" version Adam asked for, replacing round 6's all-white recolor of the newer logo art. The newer logo file (`D:\alkarama updated website.jpeg`) is no longer used anywhere; say so explicitly if he wants it back later.
- `npx tsc --noEmit` clean. Verified via preview: logo loads at the bigger nav size, all partner logos load with no broken images.
- PUSHED to `main` again on the same "push it" instruction as round 6.

## Client round 8 (2026-07-10, same day)
Adam reported Saudi/IATA/Inspiring Tunisia were STILL showing white boxes even after round 7's fix + push, and offered to manually source clean logos himself if needed. Also flagged reviews count (34 vs a stale "15" subtitle) and misalignment in the Reviews and Omra cards.
- LIKELY REAL ROOT CAUSE of the repeated "still broken" partner-logo reports across rounds 6/7/8: every fix was verified correct at the pixel level on disk, yet kept appearing broken live - strongly points to Vercel/browser HTTP caching serving the OLD file bytes under the SAME filename even after redeploys (`/public` static assets are commonly cached long-term). FIX: renamed the 6 reprocessed partner logos with a `-v2` suffix (`saudi-color-v2.png`, `tunisia-color-v2.png`, `iata-color-v2.png`, `turkiye-color-v2.png`, `ooredoo-v2.png`, `star-assurances-v2.png`), deleted the old filenames, updated `data.ts` `heroPartners` to point at the new names. This forces every cache layer to fetch fresh bytes regardless of what was cached before. If these ever need reprocessing again, bump to `-v3` - do not reuse a filename that was ever previously deployed under a wrong/broken version, that's what silently caused rounds 6-8 to loop.
- Adam offered to manually find/send clean no-background partner logos if this still doesn't resolve it - told him yes, that would remove all doubt (see reply); the current sources are low-res cleanpng.com-style downloads, official brand-kit assets would be a strict upgrade.
- REVIEWS SECTION (`Sections.tsx` `Reviews()`): subtitle was a static translated string hardcoded to "5.0 sur 15 avis Google" while the bottom pill already read the live count (34) from the content store - now both pull from `content.business.rating`/`reviews` dynamically, so they can never drift again. Also bumped the `data.ts` default seed from 15 to 34 so a fresh/reset install matches reality. Review cards: added `flex flex-col` + `items-stretch` on the grid + `md:mt-auto` on the figcaption, so the name/avatar row sits at the same height across all 3 cards regardless of how long each review's text is (previously it tracked the bottom of whatever text length that card had). Avatar swapped from a plain gold-tinted initial letter to a solid gold circle with a white person icon (`Icon.person`, the same icon added earlier for Omra headcounts) - matches the gold/charcoal branding instead of a generic letter; a real customer photo would need Adam to provide one per reviewer, not something to fabricate.
- OMRA HOMEPAGE CARDS: the earlier round-5 alignment fix only pinned the BOOK BUTTON to the bottom (`md:mt-auto` on the `<a>` alone) - but the price grid above it still moved up/down independently based on how many lines the services list wrapped to on each card, so cards could still look staggered. Now the whole price-grid-or-price-note block AND the button are wrapped in one `div` with `md:mt-auto`, so that entire lower chunk anchors to the same bottom position across all 3 cards regardless of services-list length above it.
- `npx tsc --noEmit` clean. Partner logo filename swap verified live (new `-v2.png` URLs load, `naturalWidth` confirms no broken images) and reviews subtitle confirmed dynamic ("5.0 sur 34 avis Google"). The Reviews/Omra alignment CSS could not be re-confirmed with a live pixel measurement this round (the preview browser tool was temporarily unavailable near the end of the session) - it uses the exact same flex-col + mt-auto pattern already verified working for the Omra cards in round 5, so confidence is high, but Adam should give it a visual look before considering it fully closed.
- NOT YET PUSHED - ask before pushing (round 6/7 already used up the "push without asking" allowance for this session).

## PHASE 2 BUILT (2026-07-12, local only, NOT pushed)
Made the remaining sections client-editable and added photo upload. Cloud/local auto-detect and RLS model kept intact.
- STORE (`src/store.tsx`): `Content` type extended with `heroSlides`, `heroPartners`, `destinations`, `includedServices`, `visaCats`, `visaIncludes` (all seeded from `data.ts` defaults, same shallow-merge-on-load so old saved cloud payloads stay safe). `business` already carried tiktok/x/youtube/featuredVideoUrl, now surfaced in the admin.
- CONSUMERS REWIRED to read from the store instead of static `data.ts` imports: `Hero.tsx` (slides + partners, with an empty-list guard so the client cannot break the hero by deleting every slide), `Sections.tsx` (Visas, TravelPacks/destinations, Connect social links), `VisasPage.tsx`, `DestinationsPage.tsx`, `SocialRail.tsx`, `Navbar.tsx` (phone). `data.ts` unchanged, still the seed/defaults. `BookingFormPage.tsx` left on static `business` (only uses phoneIntl via `wa()`, which itself still reads static phoneIntl. Known pre-existing limitation, noted below).
- ADMIN (`src/pages/AdminPage.tsx`): 3 new tabs plus extended Coordonnees.
  - "Accueil (hero)": edit each existing slide (service badge, headline, tagline, background photo, offer-card photo, card badge + urgent toggle + title + sub + price + WhatsApp message). Per Adam's choice this round: EDIT-EXISTING-ONLY, no add/delete/reorder of slides (safer, cannot end up with an empty carousel). Also a partners-logo editor below (add/delete, logo upload or text wordmark).
  - "Destinations": full add/edit/delete (title, desc, photo, price, WhatsApp msg, "show on homepage" + "PROMO ribbon" toggles).
  - "Visas": edit the shared "included in your file" bullet list, plus full add/edit/delete of categories (name, photo, country/type list).
  - "Coordonnees" now also edits TikTok, X, YouTube profile links (blank hides the icon automatically) on top of the existing Facebook/Instagram/featured-video fields.
- PHOTO UPLOAD (`src/upload.ts` + new `ImageField` component in AdminPage): replaces every image-path text field (hotels, packs, hero backgrounds + card images, destinations, visas, partners). Picks a photo, compresses it in the browser with a plain `<canvas>` (resize longest side to 1600px, re-encode JPEG q0.82, keep original if that would be bigger. No new npm deps), uploads to a public Supabase Storage bucket `site-images`, stores the returned public URL in the content JSON. Backward compatible: existing `/public/...` paths still render, and the field keeps a manual path input as a fallback. In LOCAL mode the upload button is hidden and only the path input shows (upload needs the cloud bucket).
- STORAGE SETUP (one manual step, in `supabase/schema.sql`, needs Adam/client to run): Dashboard -> Storage -> New bucket named `site-images`, "Public bucket" ON. Then run the appended SQL block (3 storage RLS policies: authenticated insert/update/delete on that bucket, plus public read). Until this is done, image fields gracefully stay as path inputs, everything else in phase 2 works.
- VERIFIED: `npx tsc --noEmit` clean. Public homepage renders entirely from the store (hero slides, partners, omra packs, destinations, visas) with no console errors. Admin: temporarily forced LOCAL mode (moved `.env.local` aside, restored after) to log in and click through, since the live cloud login needs the client's password. All 7 tabs mount, the 3 new editors render, and the full edit -> Enregistrer -> reload -> persists loop was confirmed (set the X social link, it saved to storage, survived reload, and the previously-hidden X icon then appeared on the public SocialRail + footer). Test data cleared, `.env.local` restored to cloud mode afterward.
- STILL TO DO by Adam before the client uses uploads live: create the `site-images` bucket + run the storage SQL (above), then log into the real cloud /admin, upload one photo end to end, and do one edit -> Enregistrer -> reload persist check in the cloud. NOT pushed to git (per Adam's instruction this session).
- LOCKDOWN (2026-07-12, per Adam): the client must only EDIT existing things, never break the site by adding/deleting sections. Add and delete buttons are now REMOVED everywhere EXCEPT the two things Adam wants growable: Umrah packs (both "Formules Omra" and "Autres departs" tabs) and Destinations/service packs. Edit-only now: Hotels, Visa categories (+ their country tags and the shared "included" bullet list are locked to text-edit only), Hero slides (already), Hero partner logos. Verified in local mode: Hotels/Visas/Partners show zero add/delete buttons; Umrah + Destinations still have them.
- Pre-existing limitation still open (not introduced here): `wa()` deep links use the fixed `business.phoneIntl` from `data.ts` even if the phone is edited in the panel. Wire `wa()` through the store if the client needs the WhatsApp number itself editable.

## PENDING - frontend
- Amadeus logo missing (text only). Adam to provide or leave as text.
- Background photo for the homepage Omra packs section (dark charcoal section): Adam said he'd send a separate photo for this, not yet received.
- Hotels full listing page only shows the current 3 real hotels (Adam confirmed this is fine for now); add more when he sends real data.
- Hotel search is a client-side filter only; Adam wants it eventually wired to his real hotel booking/availability system (unspecified which one yet) - ask him which system when that phase starts.
- A "Nos Services" page (nav item planned) - not built yet.

## Deployment (LIVE since 2026-07-02)
- GitHub repo: https://github.com/adamtrax2-lang/alkarama-ibnsina (Adam's own account, created for this project). Connected to Vercel with auto-deploy: every push to `main` redeploys automatically, no manual Vercel step needed after the initial import.
- KNOWN QUIRK: this Windows machine has git credentials cached for a DIFFERENT GitHub account (`adamscalesai`), not `adamtrax2-lang`. A plain `git push` will fail with 403. Fix used so far: ask Adam for a fresh classic Personal Access Token from github.com/settings/tokens (repo scope, logged in as adamtrax2-lang), then `git remote set-url origin https://<token>@github.com/adamtrax2-lang/alkarama-ibnsina.git`, push, then `git remote set-url origin https://github.com/adamtrax2-lang/alkarama-ibnsina.git` to remove the token from the stored config again. Tell Adam to revoke the token after use.
- Current deployed commits: `858debd` (initial frontend) + `346d809` (mobile date-field fix). Only frontend fixes have been pushed so far - the backend/Supabase work described below is still local-only (uncommitted), intentionally, since it is not finished/tested yet. Do NOT `git add -A` blindly - check `git status`/`git diff --stat` first and stage only what's actually ready to ship, so half-finished backend work doesn't go live by accident.
- Recommended: separate Vercel account (or at least a separate Team) from Adam's personal one - keeps usage/limits and ownership separate, and makes it easy to hand off access to the client later if ever needed.
- Heads up on Vercel's free "Hobby" tier: its terms are technically for personal/non-commercial use. Using it to host a paid client's site is extremely common among freelancers and low risk for a small low-traffic site like this, but it is not strictly "by the book." If Adam wants to be fully compliant, Cloudflare Pages' free tier explicitly allows commercial use, or Vercel Pro (~$20/mo) is the sanctioned commercial option. Charging the client an ongoing "hosting/maintenance" fee while the underlying platform costs Adam nothing is normal agency practice either way - not an issue by itself.
- Domain: not yet connected. After Adam buys it, add it in the Vercel project's Domains settings (standard DNS/CNAME step).

## Backend admin panel (DEMO built 2026-07-02, local-first)
- Goal for the client meeting: a working, clickable admin panel that proves he can self-edit packs/dates/prices with no code. Built local-first (saves in the browser) so it needs zero accounts and demos instantly. The database swap is a later, isolated change.
- Data layer is isolated in `src/store.tsx` (`ContentProvider` + `useContent()`). Today it seeds from `src/data.ts` (the defaults) and persists edits to `localStorage` (key `alkarama_content_v1`). ONLY three functions in that file (loadContent / save / reset) touch storage. Swapping to Supabase = rewrite those three to async DB calls; the admin UI and the public site do NOT change.
- Store currently manages: `umrahPacks`, `umrahMoreDepartures`, `hotels`, `business`. Public consumers now read from the store: `Sections.tsx` (Omra, Hotels, Reviews, Contact, Footer), `OmraPage`, `HotelsPage`. Everything else (visas, hero, reviews text, whyUs, billets, `wa()` WhatsApp links) still reads static from `src/data.ts` (not yet client-editable). Note: `wa()` deep links keep the fixed `business.phoneIntl` even if the phone is edited in the panel, wire that up when needed.
- Admin at `/admin` (App.tsx renders it as a standalone shell: no public navbar/footer/social rail). `src/pages/AdminPage.tsx`. Tabs: Formules Omra (accueil) / Autres departs / Hotels / Coordonnees. Full add/edit/delete of packs (bilingual name, dates, duration, stars, highlight toggle, services list, prices-by-occupancy) and hotels; contact info editor. "Enregistrer" (dirty-aware), "Voir le site", "Reinitialiser tout", "Se deconnecter".
- Admin UI language is FRENCH ONLY (internal tool, one French-speaking user). The CONTENT it edits stays bilingual FR+EN (both fields editable per string).
- Verified end to end via preview (local mode): login works, edit a pack price -> Enregistrer -> persists -> survives full reload -> homepage renders the new price. Demo data reset to defaults after testing. `npx tsc --noEmit` clean, no console errors.

## Supabase backend (CODE DONE 2026-07-02, auto-detect cloud/local)
- Installed `@supabase/supabase-js`. The store now runs in one of TWO modes, chosen automatically at runtime:
  - CLOUD: when `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY` env vars are present. Reads/writes one JSONB row (`site_content`, id `main`) in Supabase. Real Supabase Auth login (email+password) in /admin. Shared across all devices/visitors.
  - LOCAL: when the keys are absent (current state). Reads/writes localStorage, demo password gate. This is the fallback so the app never breaks before setup.
- Files: `src/supabase.ts` (client + `isSupabaseConfigured` + table constants), `src/store.tsx` (async load/save/reset, both modes), `src/pages/AdminPage.tsx` (adaptive login + Cloud/Local badge + save error handling), `supabase/schema.sql` (one table + RLS), `.env.example` (key template; real keys go in `.env.local`, already gitignored via `*.local`).
- DESIGN CHOICE: whole editable site = one JSON document in one row (not normalized tables). This is a small single-site CMS; the JSON blob maps exactly to the store's `save(whole Content)` and keeps the DB code tiny. Revisit only if we ever need relational queries (we don't).
- CLOUD mode is typecheck-clean but NOT yet verified live (needs a real project). LOCAL mode is fully verified.

## TO GO LIVE ON THE CLOUD (the only manual steps, ~10 min, all free)
1. Create a free Supabase project at supabase.com (New project; pick a region near Tunisia e.g. EU; set a DB password, save it).
2. SQL Editor -> paste all of `supabase/schema.sql` -> Run.
3. Authentication -> Users -> Add user -> the client's email + a password (this is his /admin login). Optionally disable "Confirm email" so it's active immediately.
4. Project Settings -> API -> copy the Project URL and the `anon public` key. Put them in `.env.local` (copy from `.env.example`). Restart `npm run dev`.
5. Add the SAME two vars in Vercel (Project -> Settings -> Environment Variables) and redeploy, so the live site is cloud-backed too.
6. Then re-verify: /admin shows a "Cloud" badge + email login; a saved edit appears on another device/browser.

## STILL PENDING - backend niceties (after cloud is live)
- Photo upload from the panel (Supabase Storage). Today the hotel image field just points at an existing /public path.
- Extend the store to cover sections still static if the client wants them editable: visas, hero slides, reviews text, whyUs, billets. (Currently editable: umrahPacks, umrahMoreDepartures, hotels, business.)
- `wa()` WhatsApp links still use the fixed `business.phoneIntl` even if the phone is edited in the panel; wire up if needed.
- Deliver a short Loom showing the client how to use the admin panel.

## PHASE 2 - full admin editability (agreed 2026-07-09, NOT built yet, deferred by Adam)
Cloud backend is LIVE and verified (Supabase project ttpxajguqlvtmaovpvww, /admin cloud login works, saves persist, confirmed a real price edit landed in the DB). Adam demoed the working core at the client meeting. The client wants to self-edit EVERYTHING; today only part of it is editable. Build the rest as a paid phase 2.
- CURRENTLY editable (store + AdminPage tabs): umrahPacks (Omra homepage), umrahMoreDepartures (/omra), hotels (homepage + /hotels, text/price only), business (name, phones, address, rating, Facebook + Instagram links).
- NOT yet editable, client explicitly asked for all of these:
  - Hero section: the rotating service slides (text + images).
  - Hero small offer card (Schengen visa / Umrah Mawlid "a partir de 4250" / Cairo-Sharm offers).
  - Visas: homepage section AND /visas full page incl. country lists.
  - Destinations / travel packs.
  - YouTube video (change the link/which video shows).
  - Social links beyond FB/IG: TikTok, X, YouTube (fields exist in `business` in data.ts but are NOT in the Coordonnees admin tab, only facebook + instagram are).
  - Photos everywhere (needs Supabase Storage upload + client-side image compression; hotel img is a path text field today, packs have no image field).
- Build = extend `src/store.tsx` Content + new AdminPage tabs for each section, plus the Storage photo-upload feature. Straightforward but sizable; must be tested before pushing live (do NOT rush it before a demo).
- Admin access decision: NO public login button on the site. Client just bookmarks /admin (hidden gate, standard + safer).

## Business / monetization notes (from Adam, for continuity)
- Deal so far: 2000 TND one-time (1150 frontend + 850 admin backend). Only recurring line currently is a small hosting fee. Adam wants real monthly recurring revenue (MRR).
- Hotel booking API integration: client uses some Tunisian hotel-booking system (NAME UNKNOWN, ask at meeting) and wants visitors to see his real admin-panel prices and message him to book. This is a SEPARATE paid item, price it as setup fee + monthly (it runs a live service that drives his bookings). Blocked until we know which system it is and whether it exposes an API/feed + credentials.
- Separate/bigger project: a multi-office bookings-left dashboard for the whole brand (multiple offices tracking remaining seats per Omra/Hajj package). License per office/seat, monthly = the real MRR play. Adam may mock this in Manus.ai for the pricing conversation.

## Verify without burning tokens
- `npx tsc --noEmit` for types. DOM checks via preview_eval. Screenshot tool has been flaky/timing out; prefer letting Adam view in the browser.
- Do NOT re-download or re-process images already in public/.
